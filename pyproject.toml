[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "zeta-zeros"
version = "0.3.0"
description = "Reproducible verification for a 67.3195% lower bound on simple zeta zeros on the critical line"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "npip99" }, { name = "ainta" }, { name = "Tormod Haugland" }]
dependencies = ["python-flint>=0.6,<1"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Mathematics",
]

[project.scripts]
zeta-zero-verify = "zeta_simple_zeros.cli:main"
zeta-ext-verify = "zeta_ext.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
